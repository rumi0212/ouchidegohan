<div class="container">
 
  <div class="row mt-5">
    <h2 class="mx-auto mb-5">会員一覧</h2>
  </div>
  
  <div class="row d-flex justify-content-center">
    <%= render 'index', customers: @customers%>
 </div>
 <div class="d-flex justify-content-center"><%= paginate(@customers)%></div>
</div>