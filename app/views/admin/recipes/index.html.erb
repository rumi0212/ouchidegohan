<div class="container">
 
  <div class="row mt-5">
    <h2 class="mx-auto mb-5">レシピ一覧</h2>
  </div>
  
  <div class="row d-flex justify-content-center">
  <%= render 'index', recipes: @recipes%>
 </div>
 <div class="d-flex justify-content-center mb-"><%= paginate(@recipes)%></div>
</div>