<div class="container">
 
  <div class="row mt-5">
    <h2 class="mx-auto mb-5">店舗一覧</h2>
  </div>
  
  <div class="row d-flex justify-content-center">
  <%= render 'index', stores: @stores%>
 </div>
 <div class="d-flex justify-content-center"><%= paginate(@stores)%></div>
</div>